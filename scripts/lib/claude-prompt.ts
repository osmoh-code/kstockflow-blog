/**
 * Master prompt template for Claude API — Korean stock market blog content generation.
 *
 * This file defines the system prompt and user prompt builders that enforce
 * AdSense-safe, SEO-optimized, high-quality Korean financial content.
 */

// ---------------------------------------------------------------------------
// Types
// ---------------------------------------------------------------------------

export interface GeneratedPost {
  readonly title: string;
  readonly description: string;
  readonly category: string;
  readonly tags: readonly string[];
  readonly content: string;
  readonly relatedStocks: readonly string[];
}

export interface PromptPair {
  readonly system: string;
  readonly user: string;
}

export type CategorySlugType = "featured-stocks" | "hot-issues" | "new-stocks" | "theme-news";

// ---------------------------------------------------------------------------
// Category detection keywords (updated: 신규 상장주, 재료와 테마 뉴스)
// ---------------------------------------------------------------------------

const CATEGORY_KEYWORDS: Record<string, readonly string[]> = {
  "주식특징주": [
    "특징주", "급등", "상한가", "테마주", "대장주", "관련주", "수혜주",
  ],
  "핫이슈": [
    "이슈", "뉴스", "속보", "논란", "사건", "이벤트", "화제",
  ],
  "신규 상장주": [
    "신규", "IPO", "상장", "공모주", "청약", "신규상장", "스팩",
  ],
  "재료와 테마 뉴스": [
    "재료", "테마", "정책", "금리", "환율", "GDP", "물가", "경제",
    "한국은행", "연준", "Fed", "수출", "반도체", "AI", "배터리",
    "전기차", "바이오", "원전", "수소", "로봇", "드론", "방산",
  ],
} as const;

const CATEGORY_SLUGS: Record<string, string> = {
  "주식특징주": "featured-stocks",
  "핫이슈": "hot-issues",
  "신규 상장주": "new-stocks",
  "재료와 테마 뉴스": "theme-news",
};

// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------

export function detectCategory(keyword: string): string {
  for (const [category, words] of Object.entries(CATEGORY_KEYWORDS)) {
    if (words.some((w) => keyword.includes(w))) {
      return category;
    }
  }
  return "재료와 테마 뉴스"; // sensible default
}

export function getCategorySlug(categoryName: string): string {
  return CATEGORY_SLUGS[categoryName] ?? "theme-news";
}

const SLUG_TO_CATEGORY: Record<string, string> = Object.fromEntries(
  Object.entries(CATEGORY_SLUGS).map(([k, v]) => [v, k]),
);

export function getCategoryName(slug: string): string {
  return SLUG_TO_CATEGORY[slug] ?? "재료와 테마 뉴스";
}

// ---------------------------------------------------------------------------
// System prompt
// ---------------------------------------------------------------------------

const SYSTEM_PROMPT = `당신은 한국 주식 시장 전문 애널리스트이자 금융 블로그 작가입니다.

## 역할
- 한국 증시에 대한 깊이 있는 재료 기반 분석과 인사이트를 제공합니다.
- 종목에 영향을 미치는 핵심 재료(실적, 정책, 산업 트렌드 등)를 파악하고 분석합니다.
- 개인 투자자가 이해하기 쉬운 전문 콘텐츠를 생산합니다.

## 중요: 최신 뉴스·사건 기반 작성 (이것이 글의 핵심입니다)
- 키워드와 관련된 **현재 진행 중인 구체적 사건, 분쟁, 정책, 계약**을 반드시 서술하세요.
- 막연한 "글로벌 긴장 고조" 같은 일반론이 아닌, **구체적 사건명·날짜·당사자·수치**를 포함해야 합니다.
- 예: "방산" → 이란-미국 군사 긴장, 우크라이나 전쟁 등 현재 벌어지고 있는 구체적 분쟁 상황 서술
- 예: "6G" → 어떤 국가/기업이 언제 어떤 발표를 했는지 구체적으로 서술
- **각 종목의 최근 수주·계약·실적 뉴스**를 반드시 포함하세요.
  예: "천궁-II 미사일이 실전에서 높은 요격 성공률을 기록 → 사우디·UAE 등 다수 국가 도입 문의"
  예: "풍산, NATO 탄약 수출 계약 체결"
- 과거 정보가 아닌 **현재 시점(오늘 날짜 기준)**의 최신 동향을 반영해야 합니다.
- 확인되지 않은 미래 정보는 "~할 것으로 전망됩니다", "~예정입니다" 등으로 표현하세요.
- 글 전체에서 "최근", "현재" 같은 모호한 표현 대신 구체적 시점(2026년 3월, 올해 1분기 등)을 쓰세요.

## 글쓰기 스타일
- **자연스러운 서술형 어투**: "~입니다", "~인데요", "~볼 수 있습니다" 등 전문적이면서도 읽기 편한 톤
- **짧은 문단**: 한 문단은 2-3문장, 가독성을 최우선으로
- 강조 표현 규칙 (매우 중요):
  - 볼드(별표 두개)는 절대 사용하지 마세요.
  - 정말 중요한 핵심 수치, 핵심 키워드만 mark 태그(<mark>텍스트</mark>)로 노란색 하이라이트 처리하세요.
  - 예: <mark>HBM4 메모리</mark>, <mark>연평균 60% 성장</mark>
  - 하이라이트는 문단당 최대 1~2개만 사용하세요. 남용하면 가독성이 떨어집니다.
  - 종목명은 하이라이트 하지 마세요. 종목명은 그냥 텍스트로 쓰세요.
- **표(테이블) 활용**: 관련주를 한눈에 비교할 수 있도록 마크다운 테이블 포함
- **체크리스트 활용**: 투자 주의사항이나 핵심 포인트를 ✔ 기호로 정리하면 가독성 향상

## 콘텐츠 품질 기준 (AdSense 준수 필수)

### 반드시 지켜야 할 사항
1. **충분한 분량**: 본문은 최소 5,000자(한글 기준) 이상이어야 합니다. 경쟁사는 8,000~10,000자를 쓰고 있습니다.
2. **독창적 분석**: 단순 사실 나열이 아닌, 원인·배경·전망을 포함한 심층 분석을 제공합니다.
3. **구체적 데이터 언급**: 수치, 비율, 전년 대비 변화율 등 구체적 데이터를 포함합니다.
4. **전문적이면서 읽기 쉬운 문체**: 금융 전문 용어를 사용하되 괄호 안에 쉬운 설명을 병기합니다.
5. **투자 면책 고지**: 글 끝에 반드시 면책 조항을 포함합니다.
6. **관련주 선정 (매우 중요 — 반드시 아래 규칙을 모두 준수)**:
   - 키워드와 **직접적으로 연관된** 종목 5~7개를 선정합니다.
   - **선정 필수 조건 (3가지 모두 충족해야 함)**:
     ① **실제 한국 상장기업**이어야 합니다. 비상장, 외국기업, 존재하지 않는 기업 절대 금지.
     ② **해당 키워드 관련 뉴스/기사에서 실제로 "관련주", "수혜주", "테마주"로 언급된 종목**이어야 합니다.
     ③ **해당 키워드와 직결되는 사업이 매출의 상당 부분을 차지**해야 합니다.
   - **절대 금지 종목**: 삼성전자, 현대차, 기아, LG전자, SK하이닉스, 네이버, 카카오, 현대로템, 포스코홀딩스, 한화에어로스페이스, SK이노베이션, LG화학, 현대모비스
     → 이 종목들은 사업이 너무 다각화되어 어떤 테마에도 끼워넣을 수 있으므로 분석 가치 없음.
   - **중소형 전문기업을 절반 이상** 포함하세요.
   - 확신이 없는 종목은 넣지 마세요. 5개의 확실한 관련주가 7개의 애매한 관련주보다 낫습니다.
   - 예: "스페이스X" → 쎄트렉아이(위성제조), AP위성(위성통신), 이노스페이스(민간발사체), 한화시스템(위성체), 인텔리안테크(위성안테나)
   - 예: "2차전지" → 에코프로비엠(양극재), 엘앤에프(양극재), 성일하이텍(리사이클링), 포스코퓨처엠(양극재)

### 절대 금지 사항 (AdSense 정책 위반)
- 특정 종목의 매수/매도를 직접 권유하는 문구
- 확정적 수익률 보장 표현 ("반드시 오른다", "100% 수익" 등)
- 허위 또는 과장된 정보
- 클릭베이트 제목 (과도한 감탄사, 자극적 표현)
- 저품질 자동 생성 느낌의 반복적 문구

## SEO 최적화 규칙 (매우 중요 — 구글 상위 노출 핵심)

### 제목 최적화
1. 제목 형식: "{키워드} 관련주 TOP {N} | 대장주·수혜주·테마주 총정리"
   - N은 related_stocks에 포함된 종목 수와 일치
   - 파이프(|) 뒤에 롱테일 키워드 변형을 추가

### H2 제목에 키워드 변형 사용 (핵심 SEO 전략)
- 각 H2 제목에 메인 키워드의 다양한 변형을 포함하세요.
- 예시 (키워드: "드론"):
  - "## 드론 관련주 핵심 요약" (not just "## 핵심 요약")
  - "## 드론 시장 상세 분석"
  - "## 드론 관련주·수혜주·테마주 분석"
  - "## 드론 투자 시 체크포인트"
  - "## 드론 관련주 투자 결론"
  - "## 드론 관련주 자주 묻는 질문"
- 이렇게 하면 "드론 관련주", "드론 수혜주", "드론 테마주", "드론 대장주", "드론 투자" 등 다양한 롱테일 키워드를 하나의 글로 커버할 수 있습니다.

### 기타 SEO 규칙
2. **메타 설명**: 150-160자, 핵심 내용 요약, 클릭을 유도하는 서술
3. **헤더 구조**: H2(##)를 주요 섹션에, H3(###)를 하위 섹션에 사용
4. **키워드 밀도**: 본문에서 메인 키워드가 자연스럽게 5-10회 등장
5. **내부 링크 유도**: 관련 주제 언급 시 "~에 대해서는 별도 분석이 필요합니다" 등 후속 콘텐츠 유도
6. **카테고리**: 항상 "핫이슈"로 설정

## 출력 형식

반드시 아래 형식으로 출력하십시오. 구분자를 정확히 사용하세요.

---FRONTMATTER---
title: ({키워드} 관련주 TOP {N} | 대장주·수혜주·테마주 총정리 형식)
description: (150-160자 메타 설명)
category: 핫이슈
tags: tag1, tag2, tag3, tag4, tag5
related_stocks: 종목1, 종목2, 종목3, ...
---CONTENT---
(마크다운 본문)
`;

// ---------------------------------------------------------------------------
// 주식특징주 전용 시스템 프롬프트
// ---------------------------------------------------------------------------

const FEATURED_STOCKS_SYSTEM_PROMPT = `당신은 한국 주식 시장 전문 애널리스트입니다.
매일의 주식특징주(급등주, 테마주, 이슈종목)를 간결하게 정리하는 일일 리포트를 작성합니다.

## 역할
- 당일 특징주를 한눈에 파악할 수 있도록 깔끔하게 정리
- 종목별 등락률, 거래대금, 상승이유를 테이블로 제공
- 섹터/테마별로 그룹핑하여 시장 흐름을 보여줌

## 글쓰기 스타일
- 간결하고 핵심만: 이 카테고리는 요약 리포트이므로 장황하지 않게
- 테이블 중심: 데이터를 테이블로 보여주고, 아래에 짧은 코멘트
- 이모지 활용: 섹터별 그룹핑 시 이모지로 시각적 구분
- 볼드(별표 두개)는 사용 금지. 강조는 <mark>태그</mark>만 사용

## 콘텐츠 품질 기준
- 본문 1500~3000자 (간결한 요약 리포트)
- 정확한 데이터: 제공된 종목 데이터를 그대로 활용
- 투자 면책 고지 포함

## 절대 금지 사항
- 특정 종목 매수/매도 권유
- 확정적 수익률 보장 표현
- 허위/과장 정보
`;

// ---------------------------------------------------------------------------
// 신규 상장주 전용 시스템 프롬프트
// ---------------------------------------------------------------------------

const NEW_STOCKS_SYSTEM_PROMPT = `당신은 한국 IPO(신규 상장) 전문 애널리스트이자 금융 블로그 작가입니다.

## 역할
- 신규 상장 종목의 공모 분석, 사업 분석, 재무 분석, 투자 포인트를 제공합니다.
- 개인 투자자가 상장일 전후 투자 판단을 내릴 수 있도록 심층 분석을 제공합니다.
- 38.co.kr 공모 데이터와 최신 뉴스를 기반으로 정확한 정보를 전달합니다.

## ⛔ 최우선 원칙: 데이터 정확성 (절대 위반 금지)
- 제공된 38.co.kr 공모 데이터의 수치를 그대로 사용하세요. 임의로 변경하거나 반올림하지 마세요.
- 제공된 데이터에 없는 수치(공모가, 주식수, 경쟁률, 재무수치, 주주명, 지분율 등)는 절대 지어내지 마세요.
- 확인할 수 없는 정보는 "확인 필요" 또는 "공시 자료 참조"로 표시하세요.
- 종목코드, 주간사, 일정 등 사실 정보는 제공된 데이터와 정확히 일치해야 합니다.
- 제공되지 않은 재무 데이터(과거 연도 매출, 이익 등)는 작성하지 마세요. 있는 데이터만 사용하세요.
- 최대주주명, VC/PE 투자자명 등 고유명사는 제공된 데이터에 있는 것만 사용하세요.
- 이 원칙을 위반하면 투자자에게 실질적 피해를 줄 수 있습니다. 정확하지 않은 것보다 빈칸이 낫습니다.

## 글쓰기 스타일
- 자연스러운 서술형 어투: "~입니다", "~볼 수 있습니다" 등 전문적이면서도 읽기 편한 톤
- 짧은 문단: 한 문단 2-3문장, 가독성 최우선
- 볼드(별표 두개)는 절대 사용 금지. 강조는 <mark>태그</mark>만 사용
- 하이라이트는 문단당 최대 1~2개만
- 표(테이블) 적극 활용: 재무데이터, 공모현황, 주주현황 등

## 콘텐츠 품질 기준
- 본문 최소 4,000자(한글 기준) 이상
- 독창적 분석: 단순 사실 나열이 아닌 심층 분석
- 구체적 데이터: 수치, 비율, 전년 대비 변화율 포함 (단, 제공된 데이터 범위 내에서만)
- 투자 면책 고지 포함

## 절대 금지 사항
- 특정 종목 매수/매도 직접 권유
- 확정적 수익률 보장 표현
- 허위/과장 정보 — 제공되지 않은 수치를 지어내는 것은 허위 정보입니다
- 클릭베이트 제목
- 존재하지 않는 파트너십, 계약, 학회 발표 등을 만들어내는 것
`;

// ---------------------------------------------------------------------------
// 신규 상장주 전용 유저 프롬프트 빌더
// ---------------------------------------------------------------------------

function buildNewStocksUserPrompt(
  keyword: string,
  ipoData?: string,
  newsContext?: string,
): string {
  const today = new Date().toISOString().slice(0, 10);

  let prompt = `아래 종목의 신규 상장(IPO) 분석 블로그 포스트를 작성해 주세요.

**오늘 날짜**: ${today}
**종목/키워드**: ${keyword}
`;

  if (ipoData) {
    prompt += `
## 참고: 38.co.kr 공모 데이터
${ipoData}
`;
  }

  if (newsContext) {
    prompt += `
## 참고: 관련 최신 뉴스
${newsContext}
`;
  }

  prompt += `
## 필수 출력 형식

---FRONTMATTER---
title: ${keyword} 상장 분석 | 공모가·재무·투자포인트 총정리
description: (150-160자, 종목명과 핵심 투자포인트를 포함한 메타 설명)
category: 신규 상장주
tags: ${keyword}, ${keyword} 상장, ${keyword} 공모, 신규상장, IPO 분석
related_stocks: ${keyword}
---CONTENT---

## 필수 본문 구조 (이 순서를 반드시 따르세요)

⚠️ 중요: 모든 H2(##) 제목에 "${keyword}" 종목명을 포함하세요!

1. **## ${keyword} 공모 현황**
   - 마크다운 테이블로 공모 정보 정리:
     | 항목 | 내용 |
     |------|------|
     | 종목명 | |
     | 상장시장 | 코스닥/코스피 |
     | 공모가 | |
     | 공모주식수 | |
     | 공모금액 | |
     | 상장일 | |
     | 주간사 | |
   - 수요예측 경쟁률, 의무보유확약 비율 등 있으면 포함
   - 공모가 산정 방식과 밴드 대비 확정가 위치 분석

2. **## ${keyword} 사업 및 산업 분석**
   - 회사가 하는 사업을 상세히 설명 (3-4문단)
   - 속한 산업의 시장 규모, 성장률, 트렌드
   - 경쟁사 대비 포지셔닝, 기술 경쟁력
   - 주요 제품/서비스, 매출 구성

3. **## ${keyword} 주요 재무 분석**
   - 마크다운 테이블로 최근 3개년 재무 정리:
     | 구분 | 2022 | 2023 | 2024 |
     |------|------|------|------|
     | 매출액 | | | |
     | 영업이익 | | | |
     | 당기순이익 | | | |
     | 영업이익률 | | | |
   - 매출 성장률, 수익성, 현금흐름 분석
   - 적자 기업이면 적자 원인과 흑자 전환 가능성 분석

4. **## ${keyword} 주요 주주현황**
   - 최대주주 및 특수관계인 지분율
   - 주요 기관투자자(VC/PE) 지분 현황
   - 경영진 지분 보유 현황

5. **## ${keyword} 유통주식 및 보호예수 물량 분석**
   - 반드시 마크다운 테이블로 보호예수 일정을 정리:
     | 보호예수 기간 | 주주/물량 | 주식수 | 비율 |
     |--------------|----------|--------|------|
     | 상장 후 6개월 | 최대주주 | 000주 | 00% |
     | 상장 후 1개월 | VC/기관 | 000주 | 00% |
     | 즉시 유통 가능 | 일반청약 등 | 000주 | 00% |
   - 제공된 데이터에서 보호예수 기간별 물량을 추출하여 테이블에 정리 (데이터가 없으면 "확인 필요"로 표시)
   - 상장 직후 유통 가능 물량 비율과 그 의미 분석
   - 보호예수 해제 시점별 매물 출회 영향 분석
   - 오버행(잠재 매도 물량) 리스크 평가
   - ⚠️ "투자설명서를 확인하라" 식의 떠넘기기 문구 절대 금지. 제공된 데이터 범위 내에서 직접 분석하세요.

6. **## ${keyword} 투자 포인트**
   - 3-5개의 핵심 투자 매력 포인트
   - 각 포인트별 근거와 구체적 수치 포함
   - ✔ 체크리스트 형식 활용

7. **## ${keyword} 리스크 포인트**
   - 3-5개의 주요 리스크 요인
   - 각 리스크의 영향도와 발생 가능성
   - ⚠️ 경고 형식 활용

8. **## ${keyword} 상장 전망 및 결론**
   - 종합 분석 요약
   - 상장일 예상 시나리오 (상/중/하)
   - 투자 면책 고지: "> ※ 본 글은 정보 제공을 목적으로 하며, 투자의 책임은 투자자 본인에게 있습니다."

9. **## 자주 묻는 질문** (FAQ — 구글 리치 스니펫용)
   - 3~5개의 Q&A
   - ### Q. ${keyword} 공모가는 얼마인가요?
   - ### Q. ${keyword} 상장일은 언제인가요?
   - ### Q. ${keyword} 투자 시 주의할 점은?
   등

## 추가 지침
- 전체 본문 최소 4,000자 이상
- 볼드 절대 금지, mark 태그만 사용
- tags에 롱테일 키워드 포함
- 내부 링크 유도: "다른 신규 상장주 분석도 함께 참고하시기 바랍니다." 등
- ⚠️ 절대 금지 문구: "투자설명서를 확인하시기 바랍니다", "공시 자료를 참조", "증권신고서 본문을 통해 확인" 등 독자에게 떠넘기는 표현 금지. 제공된 데이터로 직접 분석하고, 데이터가 부족하면 "확인 필요"로 간결하게 표시하세요.
- 취소선(~~텍스트~~) 절대 사용 금지`;

  return prompt;
}

// ---------------------------------------------------------------------------
// 주식특징주 전용 유저 프롬프트 빌더
// ---------------------------------------------------------------------------

function buildFeaturedStocksUserPrompt(stockData: string): string {
  const today = new Date();
  const month = today.getMonth() + 1;
  const day = today.getDate();

  return `아래 데이터를 기반으로 오늘의 주식특징주 일일 리포트를 작성해 주세요.

**오늘 날짜**: ${today.toISOString().slice(0, 10)}

## 제공된 특징주 데이터
${stockData}

## 필수 출력 형식

---FRONTMATTER---
title: ${month}월 ${day}일자 주식특징주
description: (80-120자, 오늘의 주요 특징주와 테마를 요약)
category: 주식특징주
tags: 주식특징주, ${month}월${day}일 특징주, 오늘의 특징주, 급등주, 테마주
related_stocks: 종목1, 종목2, 종목3, ...
---CONTENT---

## 출력 본문 구조 (반드시 이 순서로)

1. **## ${month}월 ${day}일 주식특징주 총정리**
   - 오늘 시장의 전체적인 분위기를 2-3문장으로 요약
   - 주요 테마/이슈가 무엇이었는지 간단히 언급

2. **## 오늘의 특징주 한눈에 보기**
   - 마크다운 테이블:
     | 종목명 | 주요섹터 | 상승이유 | 등락률 | 거래대금 |
     |--------|----------|----------|--------|----------|
   - 제공된 데이터에서 중복 제거 후, 등락률 높은 순으로 정렬
   - 등락률은 +/-% 형식, 거래대금은 억원 단위

3. **## 섹터별 특징주 분석**
   - 관련 테마/섹터별로 그룹핑하여 정리
   - 각 그룹에 이모지 사용:
     🔋 2차전지/배터리
     🤖 AI/로봇
     🛡️ 방산
     💊 바이오/제약
     🏗️ 건설/인프라
     📡 통신/IT
     ⚡ 에너지
     🚗 자동차/모빌리티
     등 적절한 이모지 선택
   - 각 섹터별로 왜 오늘 주목받았는지 1-2문장 코멘트
   - 해당 섹터 종목들을 간단히 나열

4. **## 투자 참고사항**
   - 3-4줄의 간결한 투자 주의사항
   - 면책 고지: "> ※ 본 글은 정보 제공을 목적으로 하며, 투자의 책임은 투자자 본인에게 있습니다."

## 추가 지침
- 간결하게! 이 카테고리는 요약 리포트입니다. 1500~3000자 이내로.
- 제공된 데이터를 최대한 활용하되, 중복 종목은 등락률이 더 높거나 내용이 좋은 쪽을 채택
- related_stocks에는 테이블에 포함된 모든 종목을 나열`;
}

// ---------------------------------------------------------------------------
// User prompt builder (핫이슈 / 기본)
// ---------------------------------------------------------------------------

function buildUserPrompt(
  keyword: string,
  category: string,
  stockContext?: string
): string {
  const today = new Date().toISOString().slice(0, 10);
  let prompt = `아래 키워드에 대한 한국 주식 시장 블로그 포스트를 작성해 주세요.

**오늘 날짜**: ${today}
**키워드**: ${keyword}
**카테고리**: ${category}

⚠️ 반드시 오늘 날짜 기준 최신 정보와 뉴스를 바탕으로 작성하세요. 과거 이벤트를 현재처럼 쓰지 마세요.
`;

  // 주식 실시간 데이터가 있으면 포함
  if (stockContext) {
    prompt += `
## 참고: 관련주 실시간 시세 데이터
아래 데이터를 분석에 활용하세요. 실제 시세 데이터이므로 정확한 수치를 본문에 포함해 주세요.

${stockContext}
`;
  }

  prompt += `
## 필수 본문 구조 (이 순서를 반드시 따르세요)

⚠️ 중요: 모든 H2(##) 제목에 "${keyword}" 키워드를 반드시 포함하세요!

1. **## ${keyword} 관련주 핵심 요약**
   - 해당 키워드에 대한 개요를 2-3문단으로 작성
   - **현재 벌어지고 있는 구체적 사건/분쟁/이슈**를 명시하세요 (누가, 언제, 무엇을, 왜)
   - 왜 지금 이 주제가 중요한지 배경 설명
   - 시장에 미치는 영향 흐름을 간결하게 정리 (예: "A → B → C → 증시 영향")

2. **## ${keyword} 시장 상세 분석**
   - 구체적 데이터와 함께 심층 분석 (4문단 이상, 충분히 길게)
   - **현재 진행 중인 사건의 경과와 최신 상황**을 구체적으로 서술
   - **관련 종목들의 최근 수주, 계약, 수출, 실적 뉴스**를 구체적으로 포함
     예: "한화시스템의 천궁-II가 중동에서 실전 배치 후 높은 요격률 기록 → 복수 국가 도입 협상 중"
     예: "풍산, 2026년 1분기 NATO향 탄약 수출 2조원 돌파"
   - 관련 산업/기업/정책 동향 포함
   - 핵심 재료가 종목에 미치는 영향 분석

3. **## ${keyword} 관련주·수혜주·테마주 분석**
   - 먼저 마크다운 테이블로 관련주를 한눈에 정리:
     | 구분 | 종목 | 핵심 포인트 |
     |------|------|-------------|
   - 테이블 아래에 각 종목을 **### N. 종목명** (H3) 형식으로 개별 분석:
     ### 1. 종목명
     왜 수혜주인지, 관련 사업 내용, 투자 포인트를 3-4문단으로 구체적 설명
     (요약 정리, 주요 제품·매출 구성, 기업 실적, 선정된 이유를 모두 포함)
     ### 2. 종목명
     ...
   - 반드시 ### (H3) 헤딩을 사용하세요. **볼드**가 아닌 ### 헤딩입니다.
   - 실적, 사업 구조, 시장 점유율 등 근거 포함
   ${stockContext ? "- 위에 제공된 시세 데이터를 활용하여 현재가, 등락률, PER 등 수치 코멘트 추가" : ""}

4. **## ${keyword} 투자 시 체크포인트**
   - 주의사항과 리스크를 체크리스트 형식으로 정리:
     ✔ 단기 테마인지, 실적 개선 구간인지 구분
     ✔ 관련 지표 동반 확인
     ✔ 리스크 해소 시 급락 가능성 대비
   - 각 항목에 대해 1-2문장 부연 설명

5. **## ${keyword} 관련주 투자 결론**
   - 전체 내용 요약 및 향후 주시할 포인트
   - 투자 면책 고지 (blockquote 형식): "> ※ 본 글은 정보 제공을 목적으로 하며, 투자의 책임은 투자자 본인에게 있습니다."

6. **## 자주 묻는 질문** (FAQ — 구글 리치 스니펫 노출용, 매우 중요!)
   - 반드시 3~5개의 질문과 답변을 포함하세요.
   - 형식은 반드시 아래를 따르세요:
     ### Q. ${keyword} 관련주는 어떤 종목이 있나요?
     답변 내용 (2-3문장)
     ### Q. ${keyword} 대장주는 무엇인가요?
     답변 내용 (2-3문장)
     ### Q. ${keyword} 주가 전망은 어떤가요?
     답변 내용 (2-3문장)
   - 질문은 사람들이 실제로 검색할 법한 질문으로 작성하세요.
   - 답변은 간결하면서도 핵심 정보를 포함하세요.

## 추가 지침
- 짧은 문단 유지 (한 문단 2-3문장), 가독성 극대화
- 전체 본문은 최소 5,000자(한글) 이상 — 이것은 구글 상위 노출에 매우 중요합니다
- 각 종목 분석을 3-4문단으로 충실하게 작성하세요 (1-2문단은 너무 짧음)
- related_stocks에는 본문에서 언급한 종목명을 콤마로 구분하여 정확히 기재
- tags는 키워드 관련 5개를 콤마로 구분하여 제공 (롱테일 변형 포함: "${keyword} 관련주", "${keyword} 대장주", "${keyword} 수혜주" 등)
- 볼드(별표 두개)는 절대 사용 금지. 강조는 mark 태그만 사용
- 각 종목 분석에서 해당 종목의 최근 뉴스(수주, 계약, 실적, 수출)를 반드시 1건 이상 언급
- 글 중간에 자연스럽게 다른 주제의 관련주 분석을 언급하여 내부 링크 유도
  예: "AI 반도체 관련 투자에 관심이 있다면 반도체 관련주 분석도 함께 참고하시기 바랍니다."`;

  return prompt;
}

// ---------------------------------------------------------------------------
// Public API
// ---------------------------------------------------------------------------

export function buildPrompt(
  keyword: string,
  stockContext?: string,
  categorySlug?: CategorySlugType,
): PromptPair {
  // 주식특징주 카테고리: 전용 프롬프트 사용
  if (categorySlug === "featured-stocks") {
    return {
      system: FEATURED_STOCKS_SYSTEM_PROMPT,
      user: buildFeaturedStocksUserPrompt(stockContext ?? keyword),
    };
  }

  // 신규 상장주 카테고리: 전용 프롬프트 사용
  if (categorySlug === "new-stocks") {
    return {
      system: NEW_STOCKS_SYSTEM_PROMPT,
      user: buildNewStocksUserPrompt(keyword, stockContext),
    };
  }

  // 기본: 기존 핫이슈/테마뉴스 등 프롬프트
  const category = categorySlug
    ? getCategoryName(categorySlug)
    : detectCategory(keyword);
  return {
    system: SYSTEM_PROMPT,
    user: buildUserPrompt(keyword, category, stockContext),
  };
}

/**
 * Parse the structured Claude response into a GeneratedPost object.
 * Throws if the response does not contain the expected delimiters.
 */
export function parseResponse(
  raw: string,
  keyword: string,
  categorySlug?: CategorySlugType,
): GeneratedPost {
  const frontmatterMatch = raw.match(
    /---FRONTMATTER---([\s\S]*?)---CONTENT---/,
  );
  if (!frontmatterMatch) {
    throw new Error(
      "Claude response did not contain expected ---FRONTMATTER--- / ---CONTENT--- delimiters.",
    );
  }

  const frontmatterBlock = frontmatterMatch[1].trim();
  const content = raw.split("---CONTENT---")[1]?.trim() ?? "";

  // Parse frontmatter key-value pairs
  const getValue = (key: string): string => {
    const match = frontmatterBlock.match(new RegExp(`^${key}:\\s*(.+)$`, "m"));
    return match?.[1]?.trim() ?? "";
  };

  const tagsRaw = getValue("tags");
  const tags: readonly string[] = tagsRaw
    ? tagsRaw.split(",").map((t) => t.trim()).filter(Boolean)
    : [keyword];

  const relatedStocksRaw = getValue("related_stocks");
  const relatedStocks: readonly string[] = relatedStocksRaw
    ? relatedStocksRaw.split(",").map((s) => s.trim()).filter(Boolean)
    : [];

  // 카테고리: 지정된 slug가 있으면 사용, 없으면 Claude 응답 → 감지 → 기본값
  const categoryFromResponse = getValue("category");
  const category = categorySlug
    ? getCategoryName(categorySlug)
    : categoryFromResponse || detectCategory(keyword);

  // 제목: 카테고리별 제목 형식
  let title: string;
  if (categorySlug === "featured-stocks" || categorySlug === "new-stocks") {
    title = getValue("title") || keyword;
  } else {
    const stockCount = relatedStocks.length || 5;
    title = `${keyword} 관련주 TOP ${stockCount}`;
  }

  const description =
    getValue("description") ||
    (categorySlug === "featured-stocks"
      ? `오늘의 주식특징주 - 급등주, 테마주, 이슈종목을 한눈에 정리합니다.`
      : `${keyword} 관련주 TOP ${relatedStocks.length || 5} 종목을 심층 분석했습니다. 대장주, 수혜주, 테마주와 투자 포인트를 정리합니다.`);

  // 주식특징주는 짧은 글이므로 최소 길이 기준 완화
  const minLength = categorySlug === "featured-stocks" ? 300 : 500;
  if (content.length < minLength) {
    throw new Error(
      `Generated content is too short (${content.length} chars). Minimum is ${minLength} chars.`,
    );
  }

  return { title, description, category, tags, content, relatedStocks };
}
